services:
  db:
<<<<<<< Updated upstream
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: convoai_user
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: convoai_dev
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
=======
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: convoai
      POSTGRES_USER: convoai
      POSTGRES_PASSWORD: convoai
    ports:
      - "5432:5432"
    volumes:
      - convoai_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U convoai -d convoai"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./backend
    command: sh -c "npm install && npx prisma generate && npx prisma migrate deploy && npm run dev"
    env_file:
      - ./backend/.env
    environment:
      PORT: 5050
      DATABASE_URL: postgresql://convoai:convoai@db:5432/convoai
    ports:
      - "5050:5050"
    volumes:
      - ./backend:/app
      - backend_node_modules:/app/node_modules
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
    depends_on:
      - backend

volumes:
  convoai_pgdata:
  backend_node_modules:
  frontend_node_modules:
>>>>>>> Stashed changes
